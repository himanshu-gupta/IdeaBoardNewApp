<%= form_for([@agenda,@agenda.ideas.build]) do |f| %>
  <div class="field">
    Idea <%= f.label :description %><br />
    <%= f.text_area :description, :rows => 5 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
